<template name="wetoast">
      <view class="wetoast__bd" animation="{{toastData.animationData}}">
          <block wx:if="{{toastData.title}}">
              <view class="wetoast__bd__title {{toastData.titleClassName || ''}}">{{toastData.title}}</view>
          </block>
      </view>
</template>  